package research.java.poo.interfaces;

public interface moto {
    void acelerar();
    void frear();
    void buzinar();
}
// interface é um tipo de referência em Java, semelhante a uma classe, que pode conter apenas constantes, assinaturas de métodos, métodos padrão, 
// métodos estáticos e tipos aninhados. As interfaces não podem conter implementações de métodos (exceto métodos padrão e estáticos). 
// Elas são usadas para definir um contrato que outras classes podem implementar.